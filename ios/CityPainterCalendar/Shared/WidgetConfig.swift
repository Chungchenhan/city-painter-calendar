import Foundation

enum WidgetConfig {
    static let appURL = URL(string: "https://sch.city-painter.com")!
    static let widgetAPIURL = URL(string: "https://sch.city-painter.com/api/widget-calendar")!
    static let widgetAPIToken = ""
}
